services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: lab1
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: corpus_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: